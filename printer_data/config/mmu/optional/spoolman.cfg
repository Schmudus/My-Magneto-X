[gcode_macro SPOOLMAN_ID]
description: Gate/Spool Mapping setzen
gcode:
    {% set gate = params.GATE|int %}
    {% set spool = params.SPOOLID|int %}

    MMU_GATE_MAP GATE={gate} SPOOLID={spool}
    G4 P5000
    mmu_spoolman